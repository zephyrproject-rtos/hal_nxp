# Copyright 2026 NXP
#
# SPDX-License-Identifier: BSD-3-Clause

if (CONFIG_MCUX_COMPONENT_driver.power)
    mcux_component_version(2.0.0)

    mcux_add_source(
        SOURCES fsl_power.c
                fsl_power.h
    )

    mcux_add_include(
        INCLUDES ${CMAKE_CURRENT_LIST_DIR}
    )

    mcux_add_include(
        BASE_PATH ${SdkRootDirPath}
        INCLUDES drivers/powercon
                 drivers/pdcon
                 drivers/rt2k_pmu
    )

endif()
